# rbenv.
# https://github.com/rbenv/rbenv

if [ -x "/opt/homebrew/bin/rbenv" ]; then
  init_rbenv
else
  if prompt_yn "rbenv not found. Install with \`brew install rbenv\`?"; then
    brew install rbenv
  fi
fi
